function random1(min, max) {
    let n = min + Math.floor(Math.random() * (max - min))
    return n 
}
// 30 - 70 
let x = 30 + ___
function f(callback) {
    let x = callback(40, 60)
    console.log(x) 
}
f(random1)
